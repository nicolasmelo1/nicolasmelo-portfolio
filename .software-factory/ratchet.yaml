# Frozen violations, one key per line, per rule.
# These are debt with a due date, not permission. Adding a key by hand
# to silence a new violation is the one move this file exists to make
# visible in review.
version: 1
rules:
